      <table class="table table-bordered">
       <thead>
         <tr>
           <th scope="col">今日の投稿数</th>
           <th scop="col">前日の投稿数</th>
           <th scop="col">前日比</th>
         </tr>
       </thead>
       <tbody>
         <tr>
           <td><%= @today_books.count %></td>
           <td><%= @yesterday_books.count %></td>
           <td>
             <% if @yesterday_books.count == 0 %>
             　前日の投稿が0のため計算できません
             <% else %>
               <%= @the_day_before.round %> %
             <% end %>
           </td>
         </tr>
       </tbody>
     </table>
           <table class="table table-bordered">
       <thead>
         <tr>
           <th scope="col">今週の投稿数</th>
           <th scop="col">前週の投稿数</th>
           <th scop="col">前週比</th>
         </tr>
       </thead>
       <tbody>
         <tr>
           <td><%= @week_books.count %></td>
           <td><%= @pre_week_books.count %></td>
           <td>
             <% if @pre_week_books.count == 0 %>
              前週の投稿が0のため計算できません
             <% else %>
              <%= @wow.round %> %
             <% end %>
           </td>
         </tr>
       </tbody>
     </table>